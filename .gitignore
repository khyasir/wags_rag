.env
*.env
!.env.example

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

.DS_Store

# Golden-run log. The results themselves (golden_results.md, golden_pairs.json)
# are kept on purpose — they are the review artefact.
golden_run.log
server.log
debug_chat.txt

# Real WAGS sales figures, branch and product names. This repo is public, so
# these stay local. Remove these two lines only if the repo becomes private.
golden_results.md
golden_pairs.json

# The field workbook is the human reference the registry (fields.py) was built
# from. Kept local to keep the repo clean; example_questions.md is committed.
*.xlsx
